{
  "manifest_version": 3,
  "name": "Countdown Timer Popup",
  "version": "1.0",
  "description": "A countdown timer that keeps running even when switching tabs.",
  "background": {
    "service_worker": "background.js"
  },
  "permissions": ["storage", "alarms", "windows"],
  "action": {
    "default_popup": "popup.html"
  }
}
